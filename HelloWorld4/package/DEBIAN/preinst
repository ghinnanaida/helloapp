#!/bin/sh
set -e

# Create a dedicated system user for the service if it doesn't exist
if ! id -u helloworldapp >/dev/null 2>&1; then
    adduser --system --group --home /nonexistent --no-create-home \
        --disabled-login --shell /usr/sbin/nologin helloworldapp || true
fi

exit 0